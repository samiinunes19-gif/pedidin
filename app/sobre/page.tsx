import PageContent from './page-content';

export const metadata = {
  title: 'Zé Pedidos | Sobre',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PageContent />;
}
