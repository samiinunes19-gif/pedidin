import PageContent from './page-content';

export const metadata = {
  title: 'Zé Pedidos | Política de Privacidade',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PageContent />;
}
