import PageContent from './page-content';

export const metadata = {
  title: 'Zé Pedidos | Trocas e Devoluções',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <PageContent />;
}
